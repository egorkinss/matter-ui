import {defineConfig} from 'wxt'

export default defineConfig({
  manifest: {
    name: 'MatterUI',
    version: '0.0.1',
    description: 'An extension for changing the appearance of some parts of Mattermost (private messages, command menu, etc.)'
  }
})
